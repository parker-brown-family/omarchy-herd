{
  "schemaVersion": 1,
  "id": "brownfamilysports.crook",
  "name": "Crook",
  "version": "0.2.0",
  "author": "Parker Brown",
  "license": "MIT",
  "description": "Which coding agent under herdr needs you, on the Omarchy bar",
  "kinds": ["bar-widget"],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Crook",
    "description": "One bar icon and one tray: every agent herdr is running, the ones waiting on you first, and a click that jumps to the stuck one.",
    "category": "AI",
    "allowMultiple": false,
    "defaults": {
      "hideWhenEmpty": true,
      "socketPath": ""
    },
    "schema": [
      {
        "key": "hideWhenEmpty",
        "type": "boolean",
        "label": "Hide when no agents are running",
        "defaultValue": true
      },
      {
        "key": "socketPath",
        "type": "string",
        "label": "herdr socket path",
        "defaultValue": "",
        "description": "Leave empty for the default session socket (~/.config/herdr/herdr.sock). Set this to watch a named session instead; `herdr session list` prints the path."
      }
    ]
  }
}
